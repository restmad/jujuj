<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="fill_parent"
    android:layout_height="fill_parent"
    android:orientation="vertical" >

    <LinearLayout
        android:layout_width="fill_parent"
        android:layout_height="@dimen/title_bar_height"
        android:background="@drawable/bcg_title"
        android:focusable="true"
        android:focusableInTouchMode="true" >

        <ImageButton
            style="@style/title_bar_img_btn"
            android:layout_centerVertical="true"
            android:onClick="back"
            android:src="@drawable/mt_back" />

        <EditText
            android:id="@+id/activity_searching_input"
            android:layout_width="fill_parent"
            android:layout_height="fill_parent"
            android:layout_margin="4dp"
            android:background="@drawable/edittext_selector"
            android:drawableRight="@drawable/ic_search_thin"
            android:imeOptions="actionSearch"
            android:singleLine="true"
            android:textColor="@color/white"
            android:textColorHint="@color/edit_hint"
            android:textSize="18dp" />
    </LinearLayout>

    <ListView
        android:id="@+id/activity_searching_result"
        android:layout_width="fill_parent"
        android:layout_height="fill_parent"
        android:background="@drawable/bcg_button"
        android:cacheColorHint="@color/transparent" />

</LinearLayout>