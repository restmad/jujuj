<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="fill_parent"
    android:layout_height="fill_parent" >

    <include
        android:layout_below="@+id/page_title_bar"
        android:layout_marginTop="-2dp"
        android:layout_width="fill_parent"
        android:layout_height="fill_parent"
        android:id="@+id/personal_page_content"
        layout="@layout/layout_personal_page" />

    <RelativeLayout
        android:id="@+id/page_title_bar"
        android:layout_width="fill_parent"
        android:layout_height="@dimen/title_bar_height"
        android:background="@drawable/bcg_title" >

        <ImageButton
            style="@style/title_bar_img_btn"
            android:layout_centerVertical="true"
            android:onClick="back"
            android:src="@drawable/mt_back" />

        <ToggleButton
            android:layout_centerVertical="true"
            android:id="@+id/view_tag_follow"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_alignParentRight="true"
            android:layout_marginRight="5dp"
            android:layout_marginTop="5dp"
            android:background="@color/transparent"
            android:paddingBottom="4dp"
            android:paddingLeft="8dp"
            android:paddingRight="8dp"
            android:paddingTop="4dp"
            android:textColor="@drawable/follow_style_white"
            android:textOff="@string/followed"
            android:textOn="@string/follow"
            android:textSize="15dp" />
    </RelativeLayout>
    
</RelativeLayout>